# Local environment (user overrides; .env.example is committed)
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.eggs/
dist/
build/

# Rust
target/
Cargo.lock

# Node (if used for tooling)
node_modules/

# Cache
.cache/
*.log

# makepkg artifacts
# NOTE: do NOT ignore src/ — that is the Rust source directory. makepkg's build
# src/ collides with it, so run makepkg from a clean/separate checkout.
*.tar.gz
*.pkg.tar.zst
pkg/

# OS
.DS_Store
Thumbs.db

# IDE (optional; uncomment if you prefer not to commit)
# .idea/
# .vscode/
# *.swp
# *.swo
